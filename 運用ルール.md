# **運用ルール**
**全体**
1. 機能ごとにissue(課題)を作成する
2. エラー内容や実装方針など細かくコメントに残す

**ブランチ**
・ブランチ名にはissue番号を付与する
以下の形式で統一
例) feature/issue-1_db_setup

**Git flow**
・作業ブランチは基本的にdevelopブランチから切る
・develop宛にプルリクしマージ後、mainブランチにあてる
ブランチ構成は以下
```
main
develop
  |-feature/issue(n)_~
  |-feature/issue(n)_~
etc..
```
